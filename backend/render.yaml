services:
  - type: web
    name: deck-auditor-api
    runtime: docker
    rootDir: backend
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: APP_PASSWORD
        sync: false
      - key: CORS_ALLOWED_ORIGIN
        sync: false
